# OpenShift DO280 

> Configuring a Production Cluster (OpenShift/EX280).

## Inhaltsverzeichnis

- [1. Introduction to Kubernetes and Openshift](#1-Introduction-to-Kubernetes-and-Openshift)
- [2. Kubernetes and OpenShift CLI and APIs](#2-Kubernetes-and-OpenShift-CLI-and-APIs)
- [3. Run Applications as Containers and Pods](#3-Run-Applications-as-Containers-and-Pods)
- [4. Deploy Managed and Networked Applications on Kubernetes](#4-Deploy-Managed-and-Networked-Applications-on-Kubernetes)
- [5. Manage Storage for Application Configuration and Data](#5-Manage-Storage-for-Application-Configuration-and-Data)
- [6. Configure Applications for Reliability)](#6-Configure-Applications-for-Reliability)
- [7. Manage Application Updates](#7-Manage-Application-Updates)



---


# 1. Introduction to Kubernetes and Openshift

**Einloggen**

```bash
oc whoami --show-console
https://console-openshift-console.apps.lab.example.com
```

# 2. Kubernetes and OpenShift CLI and APIs

```bash
# login mit Token (muss vorher in Web-Frontend erstellt werden)
oc login --token=sha256-fypX...Ot6A \
--server=https://api.lab.example.com:6443

# login mit username/pass
oc login -u admin https://api.lab.example.com:6443

# über webbrowser einloggen:
oc login --web api-url
```

**Infos zum Cluster erhalten**

```bash

# Cluster Version ermitteln
oc version

# URL vom Control Plane ermitteln
oc cluster-info

# alle unterstützten API Versionen anzeigen
oc api-versions

# Clusteroperatoren auflisten
oc get clusteroperator

# Status von Ressourcen ausgeben
oc status -n openshift-authentication

# verfügbare Felder und Beschreibungen anzeigen
oc explain services

```

# Kubernetes Ressourcen anzeigen 
💡**Wichtig**
- die API Group kann durch das Feld APIVERSION herausgefunden werden
- z.B. hat die Jobs Ressource die Gruppe "batch" und die Version "v1"

```bash
oc api-resources                                   # alle unterstützten Ressourcen anzeigen

oc api-resources --namespaced                      # namespaced scoped

oc api-resources --namespaced=false                # nicht namespaced scoped

oc api-resources --api-group ''                    # core API group

oc api-resources --api-group apps                  # apps API group

oc api-resources --api-group config.openshift.io  # OpenShift configuration API group

```
## Health of an OpenShift Cluster
```bash
oc get operators                                          # installierte Operatoren

oc get clusteroperators                                   # per Default istallierte Clusteroperatoren 

oc adm top pods -A --sum                                  # Zeigt Ressourcen aller Pods inklusive Summe

oc adm top pods etcd-master01 \                           # zeigt Ressourcen von allen Containern eines Pods in einem NS
-n openshift-etcd --containers

oc get node master01 -o jsonpath=\                        # Allocatable: Wieviel CPU kann noch vergeben werden
'Allocatable: {.status.allocatable.cpu}{"\n"}'\           # Capacity: Wieviel CPU Kapaziät steht überhaupt zur Verfügung
'Capacity: {.status.capacity.cpu}{"\n"}'

oc adm node-logs master01 -u crio --tail 5                # die letzten 5 Node Logs des Crio Service anzeigen

oc debug node/master01                                    # auf den Node zugreifen                          
chroot /host                                              # auf Host binaries zugreifen
systemctl status kubelet                                  # Status des kubelets anzeigen

oc adm must-gather --dest-dir /home/student/must-gather   # Diagnose und Debugging Infos sammeln

oc adm inspect clusteroperator/openshift-apiserver \      # Debugging Infos vom api-server sammeln (letzte 5 min)
--dest-dir /home/student/inspect --since 5m

```

# 3. Run Applications as Containers and Pods

**Unterschied Deployment vs. oc new-app**
- Erzeugte Objekte: new-app erzeugt auch Service bzw. Imagestream
- Environment Variablen: env kann ich nur in new-app angeben, in Deployment nur im Nachgang
- Quellen: new-app kann aus Image, Git, Template und lokalen Verzeichnis deployed werden
- Automatisierung: ist in new-app hoch 

**Allgemeine Regeln UID/GUI in OpenShift**

- UID wird durch Openshift überschrieben, wenn Standard-SCC restricted genutzt, dabei wird UID aus projektspezifischen Range genutzt (z.B. 1000680000–1000689999, pro Namespace unterschiedlich)
- Der im Dockerfile gesetzte User (z.B. 1001) wird also überschrieben, es sei denn es wird das anyuid SCC genutzt
- GID sollte auf relevanten Verzeichnissen auf root-Gruppe 0 gesetzt werden

```bash
oc attach ubi9-command -it

oc describe project pods-images | grep uid-range        # UID-Range des Projekts ausgeben

oc image info <image>                                   # Metadaten zu Image anschauen, auch CMD und Entrypoint
                                                        # z.B. User oder Home

skopeo info docker://registry/image:tag                 # Zeigt Manifest-Ebene: Name, Digest, RepoTags, Created, Labels, Layers, Architecture, Os, DockerVersion

skopeo info --config image | jq '.config.Cmd'           # zeigt Daten aus Dockerfile

oc exec -it <image> -- /bin/bash -c "whoami && id"      # UID des Users herausfinden

oc cp ~/world_x.sql \                                   # File in Container kopieren
mariadb-server:/tmp/

oc port-forward mariadb-server 3306:3306                # Weiterleitung Port 3306 Server
                                                        # auf Port 3306 im Mariadb Pod

```

# 4. Deploy Managed and Networked Applications on Kubernetes

💡**Templates**

- Templates sind nicht einfach nur ein anderer Weg zum selben Ergebnis wie Images
- sie bringen vordefinierte Best Practices mit (Health-Checks, Resource-Limits, Storage), die bei einem reinen Image-Deploy komplett fehlen und die du sonst manuell nachbauen müsstest.

```bash
oc describe template mysql-persistent -n openshift

oc new-app --name mysql --template mysql-persistent -l team=red \   # neue App auf Basis von Template mit Label
-p MYSQL_USER=developer -p MYSQL_PASSWORD=developer                 # Paramenter angeben

oc new-app --name mariadb -l team=blue \                            # neue App direkt vom Image deployen
--image registry.lab.example.com:8443/rhel10/mariadb-118:latest \
-e MYSQL_USER=developer \
-e MYSQL_PASSWORD=developer \
-e MYSQL_ROOT_PASSWORD=redhat


```
**Deployment vs. Job**

- unterschiedliches Selbstheilungsverhalten
- Deployment: Pod löschen → wird automatisch neu erstellt weil der Deployment-Controller ständig dafür sorgt, dass die gewünschte Anzahl Replicas läuft
- Job: Pod löschen, nachdem der Job bereits abgeschlossen ist → wird nicht neu erstellt weil der Job sein Ziel (COMPLETIONS: 1/1) schon erreicht hat — Job wird nicht gelöscht
- Job: "restartPolicy: Never" - Deployment: "restartPolicy: Always"

## Kubernetes Pod and Service Networks

<details>
<summary>Ausklappen Pod Networks</summary>

- Jeder Pod bekommt automatisch eine eigene IP aus einem gemeinsamen CIDR-Bereich – alle Pods liegen im selben flachen Subnetz, daher ist kein NAT für Pod-zu-Pod-Kommunikation nötig, auch nicht über Node- oder Namespace-Grenzen hinweg.
- Container innerhalb eines Pods teilen sich IP und MAC-Adresse und erreichen sich gegenseitig über localhost – das ist aber nur pod-intern sichtbar.
- Pod-IPs sind instabil: Bei Neustart, Skalierung oder Ausfall vergibt Kubernetes eine neue IP.
- Service bekommt stabile IP und fungiert als virtueller LB - fällt ein Pod aus aktualisiert Kubernetes automatisch die Service Endpoints
- Services wählen ihre Ziel-Pods über Label-Selectoren
- FQDN des Service: SVC-NAME.PROJECT-NAME.svc.CLUSTER-DOMAIN 
- /etc/resolf.conf wird bei Podstart automatisch durch DNS-Operator/Kubelet generiert
</details>

```bash
oc expose deployment/<deployment-name> [--selector <selector>]                      # Service für Deployment erstellen
[--port <port>][--target-port <target port>][--protocol <protocol>][--name <name>]

oc get endpointslices 

oc run shell --env POD_IP=$POD_IP -it --rm \                                        # Test connect auf Pod IP
--image registry.lab.example.com:8443/openshift4/network-tools-rhel8 \
--restart Never \
-- nc -zv $POD_IP 3306

oc run shell -it --rm \                                                             # Test connect auf svc in anderen NS
--image registry.lab.example.com:8443/openshift4/network-tools-rhel8 \
--restart Never -- nc -zv db-pod.deploy-services.svc.cluster.local
```

## Scale and Expose Applications to External Access

**Service-Typen**
- **ClusterIP** (Standard): nur clusterintern erreichbar, für Pod-zu-Pod-Kommunikation.
- **LoadBalancer**: provisioniert einen Load Balancer beim Cloud-Anbieter mit extern erreichbarer IP 
- **ExternalIP**: leitet Traffic von einer virtuellen IP auf einem Node an einen Pod weiter – als Legacy-Feature aus - Sicherheitsgründen nur bei Bedarf verwenden.
- **NodePort**: exponiert einen Service auf einem festen Port (Standardbereich 30000–32767) auf jedem Node – erfordert strikte - Firewall-Regeln, da direkte Verbindungen zu Nodes nötig sind.
- **ExternalName**: mappt einen Service per CNAME-Eintrag auf einen externen DNS-Namen.

**Gateway-API**
- **GatewayClass** - clusterweite Controller-Konfiguration
- **Gateway** - namespace-scoped Netzwerk-Einstiegspunkt (Listener,Ports,TLS)
- **HTTPRoute** - Regeln, wie eingehender Traffic auf Services gemappt wird

**Ingress-Objekte**
- Standard-Kubernetes-Alternative zu Routes, unterstützt HTTP, HTTPS, SNI und TLS mit SNI. 
- Wird ebenfalls vom OpenShift-Ingress-Controller verarbeitet. 
- Routes bleiben aber wegen erweiterter Funktionen und nativer Integration die bevorzugte Wahl.

**Sticky Sessions**
- Für zustandsbehaftete Anwendungen sorgt der Ingress-Controller per Cookie dafür, dass ein Client immer denselben Pod erreicht 
- konfigurierbar über oc annotate sowohl für Ingress als auch für Route-Objekte. 
- Bei Passthrough-Routes funktioniert das nicht, da der Router den verschlüsselten Traffic nicht einsehen kann.



Beispiel: Route erstellen und sticky sessions aktivieren:

```bash
oc expose deployment satir-app --name satir-svc \                   # Service
--port 8080 --target-port 8080

oc expose service satir-svc --name satir                            # Route

oc scale deployment satir-app --replicas 2                          # Skalierung

oc annotate route satir \                                           # Sticky Sessions
router.openshift.io/cookie_name="hello"
```

Beispiel: Ingress erstellen und sticky sessions aktivieren:
```bash
oc expose deployment sakila-app --name sakila-svc \                 # Service
--port 8080 --target-port 8080

oc create ingress ingr-sakila \                                     # Ingress
--rule "ingr-sakila.apps.lab.example.com/*=sakila-svc:8080"

oc scale deployment skrila-app --replicas 3                          # Skalierung

oc annotate ingress ingr-sakila \                                   # Sticky Sessions
ingress.kubernetes.io/affinity=cookie

```

Curl mit Cookie auf Client Seite:
```bash
curl ingr-sakila.apps.lab.example.com \                             # cookie wird erstellt
-c /tmp/cookie_jar

for i in {1..10}; do \                                              # Test sticky sessions
curl ingr-sakila.apps.lab.example.com -b /tmp/cookie_jar; done


```

# 5. Manage Storage for Application Configuration and Data

## Secrets

**Arten von Secrets**
- **Opaque**: beliebige Key-Value-Paare
- **Service**: account token	Token für Kubernetes-API-Auth
- **Basic**: authentication	Username/Passwort
- **SSH keys**:	SSH-Auth-Daten
- **TLS**: Zertifikat + Key
- **Docker**: config	Registry-Zugangsdaten

```bash
# Secret aus Literalen
oc create secret generic secret_name --from-literal key1=secret1 --from-literal key2=secret2

# Secret aus Dateien
oc create secret generic ssh-keys --from-file id_rsa=/pfad/id_rsa --from-file id_rsa.pub=/pfad/id_rsa.pub

# TLS-Secret
oc create secret tls secret-tls --cert /pfad/cert --key /pfad/key

# ConfigMap (oc oder kubectl, "cm" als Shortname bei oc)
oc create cm my-config --from-literal key1=config1
```

**Config Maps und Secrets um Environment Variablen einzubinden**


einzelne Env Variablen injekten
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: config-map-example-pod
  namespace: example-app
spec:
  containers:
    - name: example-container
      image: registry.example.com/mysql-80:1-237
      command: [ "/bin/sh", "-c", "env" ]
      env:
        - name: MYSQL_DATABASE
          valueFrom:
            configMapKeyRef:
              name: config-map-example
              key: database.name
        - name: MYSQL_USER
          valueFrom:
            configMapKeyRef:
              name: config-map-example
              key: database.user
              optional: true
        - name: TEST_SECRET_USERNAME_ENV_VAR
            valueFrom:
              secretKeyRef:
                name: test-secret
                key: username
        
```


alle Env Variablen injekten

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: config-map-example-pod2
  namespace: example-app
spec:
  containers:
  - name: example-container
    image: registry.example.com/mysql-80:1-237
    command: [ "/bin/sh", "-c", "env" ]
    envFrom:
    - configMapRef:
        name: config-map-example
  restartPolicy: Never
```

**configmap als Volume in bestehendes Deployment einbinden:**
```bash
oc set volume deployment/webconfig \
--add --type configmap --configmap-name webfiles \
--name webfiles-vol --mount-path /var/www/html/
```
```bash
# Secret updaten:
oc set data secret/demo-secret -n demo \
--from-file /tmp/demo/root_password
```

# 6. Configure Applications for Reliability

# 7. Manage Application Updates